export const getFirstElement = (list: string[]): string => list[0]

export const getListSize = (list: Array<string | number>): number => list.length

export const isListLong = (list: string[]): boolean => list.length >= 3
