export const getFirstElement = (fruits: string[]): string => fruits[0]

export const getListSize = (list: (string | number)[]): number => list.length

export const isListLong = (list: string[]): boolean => list.length >= 3
